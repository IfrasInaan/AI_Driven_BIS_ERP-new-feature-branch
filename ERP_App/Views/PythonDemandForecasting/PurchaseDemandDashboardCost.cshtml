
@{
    ViewBag.Title = "PurchaseDemandDashboardCost";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Purchase Cost Forecasting</h2>

@using (Html.BeginForm("PurchaseDemandDashboardCost", "PythonDemandForecasting", FormMethod.Post))
{
    @Html.AntiForgeryToken()

    <div class="form-group">
        <label for="invoiceDate">Select Month:</label>
        <input type="month" id="invoiceDate" name="invoiceDate" class="form-control" required />
    </div>

    <div class="form-group">
        <label for="productName">Select Product:</label>
        @Html.DropDownList("productName", ViewBag.Products as SelectList, "Select Product", new { @class = "form-control", required = "required" })
    </div>

    <button type="submit" class="btn btn-warning">Predict Purchase Cost</button>
}

@if (ViewBag.PredictedCost != null)
{
    <div class="alert alert-info mt-3">
        <strong>@ViewBag.PredictedCost</strong>
    </div>
}

